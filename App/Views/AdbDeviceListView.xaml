<UserControl
    x:Class="App.Views.AdbDeviceListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Padding="8">
            <Button x:Name="RefreshButton" Content="刷新" Click="RefreshButton_Click"/>
            <Button x:Name="DiscoverButton" Content="扫描" Click="DiscoverButton_Click"/>
        </StackPanel>

        <!-- 设备列表 -->
        <ListView
            x:Name="DeviceListView"
            Grid.Row="1"
            SelectionMode="Single"
            SelectionChanged="DeviceListView_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding Serial}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Model}" FontSize="12" Opacity="0.7"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding State}" FontSize="12"/>
                            <TextBlock Text="{Binding ConnectionType}" FontSize="10" Opacity="0.6"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- 无线调试连接 -->
        <StackPanel Grid.Row="2" Padding="8" Spacing="12">
            <TextBlock Text="无线调试连接" FontWeight="SemiBold"/>

            <!-- 步骤 1：首次配对 -->
            <Expander Header="步骤 1：首次配对（仅第一次需要）" IsExpanded="False">
                <StackPanel Spacing="8" Padding="8">
                    <TextBlock Text="手机操作：设置 → 无线调试 → 使用配对码配对设备"
                               FontSize="11" Opacity="0.7" TextWrapping="Wrap"/>

                    <Grid ColumnSpacing="8" RowSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="IP 地址" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="0" Grid.Column="1" x:Name="PairIpTextBox" PlaceholderText="192.168.3.4"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="配对端口" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="1" Grid.Column="1" x:Name="PairPortTextBox" PlaceholderText="39063"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="配对码" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="2" Grid.Column="1" x:Name="PairingCodeTextBox" PlaceholderText="123456"/>

                        <Button Grid.Row="3" Grid.Column="1" Content="配对" Click="PairButton_Click" HorizontalAlignment="Stretch"/>
                    </Grid>
                </StackPanel>
            </Expander>

            <!-- 步骤 2：连接设备 -->
            <StackPanel Spacing="8">
                <TextBlock Text="步骤 2：连接设备" FontWeight="SemiBold"/>
                <TextBlock Text="手机操作：返回无线调试主界面，查看顶部显示的 IP 和端口"
                           FontSize="11" Opacity="0.7" TextWrapping="Wrap"/>

                <Grid ColumnSpacing="8" RowSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="IP 地址" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="ConnectIpTextBox" PlaceholderText="192.168.3.4"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="连接端口" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="ConnectPortTextBox" PlaceholderText="41581"/>

                    <Button Grid.Row="2" Grid.Column="1" Content="连接" Click="ConnectButton_Click" HorizontalAlignment="Stretch"/>
                </Grid>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
