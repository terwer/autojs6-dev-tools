<UserControl
    x:Class="App.Views.CodePreviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="代码预览" FontWeight="SemiBold" Padding="8"/>

        <!-- 代码编辑器 -->
        <TextBox
            x:Name="CodeTextBox"
            Grid.Row="1"
            AcceptsReturn="True"
            TextWrapping="Wrap"
            FontFamily="Consolas"
            FontSize="12"
            Padding="8"
            ScrollViewer.VerticalScrollBarVisibility="Auto"/>

        <!-- 操作按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Padding="8">
            <Button x:Name="CopyCodeButton" Content="复制代码" Click="CopyCodeButton_Click"/>
            <Button x:Name="ExportButton" Content="导出 JS 文件" Click="ExportButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
