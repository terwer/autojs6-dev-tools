<UserControl
    x:Class="App.Views.PropertyPanelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="控件属性" FontWeight="SemiBold" Padding="8"/>

        <!-- 属性列表 -->
        <ScrollViewer Grid.Row="1">
            <StackPanel x:Name="PropertiesPanel" Padding="8" Spacing="4"/>
        </ScrollViewer>

        <!-- 操作按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Padding="8">
            <Button x:Name="CopyCoordinatesButton" Content="复制坐标" Click="CopyCoordinatesButton_Click"/>
            <Button x:Name="CopyXPathButton" Content="复制 XPath" Click="CopyXPathButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
